{
  "name": "uplift",
  "version": "1.0.0",
  "private": true,
  "description": "UpLift - centralized air travel platform (backend + frontend)",
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" --kill-others \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm start --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "check-db": "npm run check-db --prefix backend",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
